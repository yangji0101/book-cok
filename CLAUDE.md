# BOOK-COK

`index.html` 파일 하나로 된 독서 기록 웹앱. 서버 없음, 저장은 `localStorage`.
자세한 구조와 규칙은 `인수인계.md` 에 있다. 코드를 고치기 전에 그 파일을 읽는다.

## 손대기 전에 알아야 할 것

- 저장은 **반드시** `put(K('이름'), 값)` / `readJSON(K('이름'), 기본값)` 을 거친다.
  `localStorage` 를 직접 쓰면 조용히 실패해서 «저장했습니다» 라고 해 놓고 사라진다.
- 화면을 다시 그릴 땐 `renderLibraryFallback()` 하나만 부른다.
- 화면에 남의 글자를 넣을 땐 `esc()`.
- 색·모서리는 파일 맨 위 `:root` 변수로 정해져 있다. 새 색을 박아 넣지 않는다.
- 고친 뒤에는 브라우저로 실제 흐름을 돌려 확인한다 (Playwright + Chromium 이 깔려 있다).

## 대답하는 방법

**모든 작업 끝에는 쉬운 단어로 2줄 이내 요약을 붙인다.**

- 자세한 설명을 먼저 하고, 맨 끝에 `## 한 줄 요약` 으로 짧게 다시 적는다
- 어려운 말(리팩터링, 인스턴스, 파싱 같은 말)은 쓰지 않는다
- 무엇이 달라졌는지, 사용자가 무엇을 하면 되는지만 적는다
