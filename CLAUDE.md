# BOOK-COK 작업 규칙

이 저장소는 파일 하나(`index.html`)로 된 독서 기록 웹앱입니다. 앱 자체를 고치는 방법은 `인수인계.md` 에 있습니다.

## 발표자료는 HyperFrames 로 만든다

발표자료·슬라이드·덱·프레젠테이션·영상 요청이 오면 **`.claude/skills/hyperframes` 스킬을 먼저 읽고** 거기서 안내하는 워크플로로 갑니다.

- 슬라이드/덱 → `slideshow` 스킬
- 다른 형식(PowerPoint 등)은 사용자가 **명시적으로 그 형식을 요구할 때만** 씁니다. 그냥 "발표자료 만들어줘" 이면 HyperFrames 가 기본입니다.
- 등록된 스킬 목록과 갱신 방법은 `.claude/skills/README.md` 참고.
