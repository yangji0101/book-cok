# BOOK-COK 인수인계 — 이 앱에 기능 붙이는 법

이 파일 하나면 됩니다. **GPT에게 이 파일과 `index.html` 을 같이 올리고**, 아래 문장을 그대로 보내세요.

- **1~4장**은 사람이 읽는 순서입니다. **4장에 복사해서 쓸 프롬프트가 다 있습니다.**
- **5~9장**은 GPT가 읽는 설명서입니다. 사람이 다 이해할 필요는 없습니다.

---

## 1. 30초 요약

| | |
|---|---|
| **앱이 뭐냐** | 파일 **하나**(`index.html`)로 된 독서 기록 웹앱. 서버 없음 |
| **어디 저장되냐** | 그 폰·그 브라우저 안에만 (`localStorage`). 사람마다 칸이 나뉨 |
| **어떻게 고치냐** | GPT한테 코드를 받아 **파일 맨 아래 표시된 자리**에 붙여넣기 |
| **어떻게 올리냐** | github.com 웹에서 붙여넣고 Commit → 1~2분 뒤 내 주소에 반영 |
| **내 주소** | `https://<내아이디>.github.io/<저장소이름>/` |

---

## 2. 적용 순서 — 아이패드 + GPT

한 바퀴에 기능 **하나**입니다. 여섯 단계이고, 익숙해지면 10분이면 됩니다.

```
Ⓐ 파일 챙기기 → Ⓑ GPT에 올리기 → Ⓒ 제대로 읽었나 확인
                                      ↓
Ⓕ 폰에서 확인 ← Ⓔ 깃허브에 붙여넣기 ← Ⓓ 기능 시키기
```

---

### Ⓐ 파일 두 개를 아이패드에 챙긴다

`index.html` 과 `인수인계.md` 두 개가 필요합니다. **선생님이 나눠준 파일이 있으면 그걸 쓰세요.** 없으면 저장소에서 직접 받습니다.

1. 사파리로 내 저장소를 엽니다
2. 파일 이름을 눌러 열고, 오른쪽 위 **Raw** 를 누릅니다
3. 글자만 있는 화면이 뜹니다 → **공유 버튼** → **파일에 저장**
4. 다른 파일도 똑같이

> 화면이 좁아 버튼이 안 보이면, 주소창 왼쪽 **ᴀA** → **데스크탑 웹사이트 요청**.
> 이 앱을 고치는 동안은 계속 켜 두는 게 편합니다.

### Ⓑ GPT에 올린다

**반드시 새 대화**에서 시작합니다. 앞 대화가 섞이면 엉뚱한 걸 고칩니다.

`＋` 버튼 → **파일 업로드** → 파일 앱에서 방금 저장한 **두 개를 같이** 고릅니다.

### Ⓒ 제대로 읽었는지부터 확인한다

바로 시키지 않습니다. 먼저 이 문장을 보냅니다.

> 올린 `index.html` 은 우리가 만든 독서 앱이고, `인수인계.md` 는 그 설명서야.
> 두 파일을 읽고, **이 앱이 기록을 어디에 어떻게 저장하는지** 세 줄로 요약해 줘.

**답에 `localStorage` 와 `K(...)` 가 나와야 합니다.** 안 나오면 파일을 제대로 안 읽은 것이니, 다시 올리고 한 번 더 물어봅니다. 여기서 확인 안 하고 넘어가면 GPT가 엉뚱한 곳을 고칩니다.

### Ⓓ 기능을 시킨다

**한 번에 하나만.** 아래를 복사해서 마지막 줄만 바꿔 보냅니다. (**4장 ②**에 복사하기 쉬운 상자로 다시 있습니다)

> 나는 아이패드를 쓰고 있어. 터미널도 컴퓨터도 없어.
> `인수인계.md` 의 규칙을 지켜서 기능을 하나 넣어 줘.
>
> - `index.html` 의 **기존 코드는 절대 고치지 마.**
> - 맨 아래 **「▼▼▼ 여기부터 내 기능 ▼▼▼」 자리에 넣을 코드만** 줘.
> - 파일 전체를 다시 주지 마. **그 조각만** 줘.
> - `▼▼▼` `▲▲▲` 표시줄은 이미 파일에 있으니 **다시 넣지 마.**
> - 저장 키는 반드시 `K(...)` 를 거치고, 화면을 다시 그릴 땐 `renderLibraryFallback()` 을 불러.
>
> 넣고 싶은 기능: **(여기에 한 문장으로)**

받은 뒤에 **한 번 더** 보내면 사고가 크게 줍니다.

> 방금 준 코드에서 **동작하지 않을 곳**을 찾아 줘. 아직 고치지는 마.

이해 안 되는 코드가 있으면 그냥 두지 말고 물어봅니다. 발표 때 설명해야 합니다.

> 이거 왜 이렇게 고쳤는지 고등학생도 알아듣게 설명해 줘.

### Ⓔ 깃허브에 붙여넣는다

1. GPT 코드블록 **오른쪽 위 복사 버튼**을 누릅니다 (직접 드래그하지 마세요, 빠집니다)
2. 사파리로 저장소 → **`index.html`** 을 눌러 엽니다
3. 오른쪽 위 **연필 ✏️**
4. **맨 아래까지 스크롤**합니다. 파일이 700줄이라 좀 걸립니다
5. 이 두 줄 **사이**를 길게 눌러 **붙여넣기**

```
/* ▼▼▼ 여기부터 내 기능 ▼▼▼ */

   ← 여기에 붙여넣는다

/* ▲▲▲ 여기까지 ▲▲▲ */
```

6. 오른쪽 위 **Commit changes...** → 팝업에서 다시 **Commit changes**

> **표시줄 두 개는 그대로 둡니다.** GPT가 표시줄까지 같이 줬으면 그 부분은 빼고 붙여넣습니다. 표시줄이 두 벌이 되면 헷갈립니다.

### Ⓕ 폰에서 확인한다

1. **1~2분 기다립니다.** 바로 안 바뀌는 게 정상입니다
2. 폰에서 내 주소를 새로고침합니다
3. 안 바뀌었으면 **탭을 닫았다 다시 엽니다** (캐시)

안 되면 **8장**을 보세요. 앱을 망가뜨릴 수 없는 구조이니 마음 놓고 시도하면 됩니다.

---

## 3. 기능은 한 문장으로 적는다

**「무엇을 넣으면 무엇이 나온다」** 형태로 적으면 GPT가 헤매지 않습니다.

| 좋은 예 | 나쁜 예 |
|---|---|
| 오늘 읽은 시간을 홈 맨 위에 분으로 보여준다 | 통계 기능 넣어줘 |
| 기록을 저장할 때 별점 1~5를 같이 저장한다 | 예쁘게 만들어줘 |
| 책을 3권 완독하면 축하 메시지를 띄운다 | 게임 요소 추가 |

**한 번에 하나씩.** 세 개를 한꺼번에 시키면 깨졌을 때 어디가 원인인지 못 찾습니다.

---

## 4. 복사해서 쓰는 프롬프트

아래 상자는 **오른쪽 위 복사 버튼**을 누르면 통째로 복사됩니다. 그대로 GPT에 붙여넣으세요.

### ① 처음 — 파일을 제대로 읽었는지 확인

```text
올린 index.html 은 우리가 만든 독서 앱이고, 인수인계.md 는 그 설명서야.
두 파일을 읽고, 이 앱이 독서 기록을 어디에 어떻게 저장하는지 세 줄로 요약해 줘.
```

답에 **localStorage** 와 **K(...)** 가 나와야 합니다. 안 나오면 파일을 안 읽은 것이니 다시 올립니다.

### ② 기능 시키기 — 제일 많이 쓰는 것

```text
나는 아이패드를 쓰고 있어. 터미널도 컴퓨터도 없어.
인수인계.md 의 규칙을 지켜서 기능을 하나 넣어 줘.

규칙:
- index.html 의 기존 코드는 절대 고치지 마.
- 맨 아래 "여기부터 내 기능" 자리에 넣을 코드만 줘.
- 파일 전체를 다시 주지 마. 그 조각만 줘.
- 표시줄은 이미 파일에 있으니 다시 넣지 마.
- 저장은 반드시 put(K(...), ...) 로 하고, 화면을 다시 그릴 땐 renderLibraryFallback() 을 불러.
- 화면에 글자를 넣을 땐 esc() 를 써.
- 새 CSS 를 만들지 말고 이미 있는 .card .label .primary .secondary .bar .pill 을 써.

넣고 싶은 기능: 여기에 한 문장으로 적는다
```

### ③ 받자마자 — 검사부터 시킨다

```text
방금 준 코드에서 동작하지 않을 곳을 찾아 줘. 아직 고치지는 마.
특히 이 셋을 봐.
- 파일에 없는 함수나 없는 id 를 부르고 있지 않은지
- localStorage 를 K(...) 와 put(...) 없이 직접 쓰고 있지 않은지
- 기록이 하나도 없을 때 화면이 깨지지 않는지
```

### ④ 이해가 안 될 때

```text
이 코드가 무슨 일을 하는지 고등학생도 알아듣게 한 줄씩 설명해 줘.
어려운 말은 쓰지 마.
```

### ⑤ 붙여넣었는데 아무 변화가 없을 때

```text
이 코드를 index.html 맨 아래 확장 블록에 붙여넣었는데 아무 변화가 없어.
화면은 멀쩡하게 잘 뜨고 오류 메시지도 안 보여.
먼저 문법 오류가 있는지 확인하고, 그다음 왜 화면에 안 나타나는지 짚어 줘.
아이패드라서 개발자 콘솔은 못 봐.

(여기에 붙여넣은 코드를 그대로 붙인다)
```

### ⑥ 파일을 통째로 다시 줬을 때

```text
파일 전체는 필요 없어. 맨 아래 확장 블록에 넣을 조각만 다시 줘.
```

### ⑦ 중간이 잘려서 왔을 때

```text
중간에 생략된 데가 있어. 생략하지 말고 처음부터 끝까지 전부 줘.
너무 길면 기능을 더 작게 나눠서 다시 짜 줘.
```

### ⑧ 터미널 명령을 알려줄 때

```text
나는 아이패드라서 터미널도 명령어도 못 써.
사파리에서 화면을 누르는 것만으로 되는 방법으로 다시 알려줘.
```

### ⑨ 다 되고 나서 — 발표 준비

```text
방금 넣은 기능을 발표에서 20초 안에 설명하려고 해.
무엇을 넣으면 무엇이 나오는지, 어디에 저장되는지 두 문장으로 정리해 줘.
```

---

## 5. 앱 구조 (GPT가 읽는 부분)

파일 하나입니다. `<style>` 세 덩어리와 `<script>` 하나, 그리고 맨 아래에 **비어 있는 확장용 `<script>`** 가 있습니다. 추가 코드는 **확장용 블록 안에만** 넣습니다.

확장용 블록은 본체 스크립트가 **전부 실행된 뒤에** 돕니다. 그래서 아래 함수와 상수를 그대로 부를 수 있습니다.

### 화면

`show(id)` 로 전환합니다. `id` 는 곧 그 화면 `<section>` 의 id 이기도 합니다.

```
home       홈 (성장 카드, 숫자 카드 4개, 최근 보관함)
reading    독서 중 (타이머, 백색소음, 방해금지 배너)
record     기록 쓰기 (제목·범위·문장·생각)
library    보관함 (책 카드 목록, ＋ 책 추가)
bookdetail 책 하나의 기록 모음
signup     등록 (처음 온 사람에게만 뜬다 — 학교→학년→반→이름)
login      누구인가요? (이 기기에 등록된 사람 고르기)
more       더보기 (설정 · 화면 위 ⚙️)
admin      관리자 (암호를 넣어야 열린다)
friends    친구 책장 (친구가 보내 준 카드 모음)
share      카드 보내기 (링크 만들기)
shared     친구가 보낸 카드 (링크로 들어왔을 때)
growth     나의 성장 (성장 단계, 숫자 카드)
nfcsetup   NFC 설정 안내
```

### 저장

서버가 없습니다. 전부 `localStorage` 입니다.

**저장 키는 반드시 `K(이름)` 을 거칩니다.** **반마다, 사람마다** 칸이 나뉘어 있어서, `localStorage.getItem('bookcok_records')` 처럼 직접 쓰면 **다른 사람 칸을 건드리거나 아무 데이터도 못 찾습니다.**

```
bookcok_records__지우                       학교·반이 없을 때
bookcok_records__세종고등학교 2-4__지우      학교·반을 골랐을 때
```

반이 없으면 예전과 **똑같은 이름표**를 씁니다. 그래서 반 기능이 생겨도 이미 쌓아 둔 기록이 사라지지 않습니다.

```js
K('records')    // 독서 기록 배열
K('completed')  // { 책이름: true } 완독 표시
K('session')    // 지금 읽는 중인 세션 (없으면 null)
K('books')      // 기록 없이 보관함에만 둔 책 이름 배열
K('friends')    // 친구에게 받아서 담아 둔 카드 배열
K('pending')    // 쓰다 만 기록 (독서를 끝냈지만 아직 저장 안 한 것)
K('path')       // 고른 마스코트 'sol' 또는 'kuk'
K('wear')       // 입고 있는 옷 id
GUEST           // 지금 사람의 이름표 (문자열)
CLASS           // 지금 학교·반. 예 '세종고등학교 2-4'. 없으면 빈 문자열
```

### 데이터 모양

```js
// 기록 하나
{
  bookTitle: '데미안',      // 책 제목
  range:     '32-57',      // 읽은 범위, 빈 문자열일 수 있음
  quote:     '...',        // 오늘의 한 문장
  thought:   '...',        // 내 생각
  duration:  1830,         // 읽은 시간, 초 단위
  noise:     '🌧️ 빗소리',   // 백색소음, 안 썼으면 '없음'
  createdAt: '2026-09-07T05:00:00.000Z',  // ISO 문자열
  id:        1757...       // 저장할 때 자동으로 붙음
}

// 친구 카드 하나 — 주소에 담아 보내는 것이라 이름이 다 짧다
{
  v: 1,                    // 판 번호
  u: '지우',                // 보낸 사람
  t: '데미안',              // 책 제목
  n: 2,                    // 기록 수
  m: 45,                   // 누적 분
  d: 1,                    // 완독이면 1
  k: '3학년1반',            // 보낸 사람의 반
  q: '새는 알을 깨고 나온다', // 가장 최근 문장
  g: '나도 깨고 싶다',       // 가장 최근 생각
  at: '2026-09-07'         // 만든 날
}

// 세션 하나 (읽는 중일 때만 있음)
{
  running: true,       // 지금 시간이 흐르는 중인가
  since:   1757...,    // 마지막으로 시작/재개한 시각 (Date.now())
  acc:     0,          // 그전까지 쌓인 초
  book:    '데미안',    // 태그가 알려준 책 이름, 없으면 ''
  byTag:   true        // NFC 태그로 시작했는가
}
```

### 쓸 수 있는 함수

**저장 — ★ 여기부터 읽기**

```js
put(키, 값)          // localStorage 에 쓴다. 실패하면 false 를 돌려주고 화면에 알린다
get(키, 기본값)       // 읽는다. 없거나 막혔으면 기본값
del(키)              // 지운다
readJSON(키, 기본값)  // 읽어서 JSON 으로. 망가져 있으면 기본값
```

**`localStorage.setItem` 을 직접 쓰지 마세요.** 사파리 사생활 보호 모드나 저장 공간이 꽉 찼을 때 조용히 실패합니다. 그러면 «저장했습니다» 라고 말해 놓고 기록이 사라집니다. `put()` 은 실패를 알아채서 화면에 띄웁니다.

**기록**

```js
await getRecordsSafe()    // 기록 전부를 배열로. async 라서 await 필요
getRecordsSync()          // 같은 것을 await 없이. 간단한 계산에 편함
await addRecordSafe(rec)  // 기록 하나 넣기
await renderLibraryFallback()  // ★ 데이터가 바뀌면 이것만 부르면 화면이 다 다시 그려진다
```

`renderLibraryFallback()` 하나가 보관함·월별 그래프·숫자 카드·성장 단계를 전부 다시 그립니다. 개별 렌더 함수를 직접 부를 필요가 없습니다.

**책**

```js
getBooks()          // 내가 추가한 책 이름 배열
saveBooks(배열)      // 통째로 저장
addBook()           // 제목을 물어서 추가 (＋ 책 추가 버튼이 부르는 것)
removeBook(이름)
toggleComplete(이름) // 완독 표시 켜고 끄기
```

**학교 · 학년 · 반**

```js
CLASS               // 지금 학교·반 ('' 이면 없음). 예 '세종고등학교 2-4'
getSchools()        // 학교 목록 — 관리자 화면에서 편집한다
saveSchools(배열)
needSignup()        // 등록 화면을 보여 줄 사람인가
renderSignup()      // 등록 화면을 처음부터 그린다
signGo(0~3)         // 등록 단계 이동 (0 학교 · 1 학년 · 2 반 · 3 이름)
switchClass()       // 등록 화면을 다시 연다 (화면 위 🏫 버튼)
classQ()            // 주소에 붙일 'c=...&' 조각. 태그 주소를 만들 때 쓴다
importOld()         // 학교·반을 쓰기 전에 쌓은 기록을 지금 칸으로 가져온다
```

**쓰다 만 기록**

```js
savePending()       // 기록 화면에 쓰던 것을 적어 둔다
loadPending()       // 적어 둔 것을 꺼낸다 (반나절 지나면 버린다)
clearPending()      // 치운다 (저장에 성공하면 저절로 불린다)
```

독서를 끝내면 세션이 지워집니다. 그런데 아이폰에서는 그 직후 단축어로 화면이 넘어가고, 돌아오면 읽은 시간이 사라집니다. 그래서 **넘어가기 전에 적어 두고, 돌아오면 되살립니다.** 확장 블록에서 기록 화면을 건드린다면 이 셋을 기억하세요.

**백색소음**

```js
NOISES              // 소리 네 개의 표 (id · 이모지 · 이름)
noise(이름, 버튼)     // 고르고 재생
stopNoise()         // 끈다
setVolume(0~100)    // 소리 크기. 저장된다
checkSound()        // 진짜 소리가 나가는지 재서 알려 준다
```

`sounds/rain.mp3` `cafe.mp3` `forest.mp3` `wave.mp3` 를 넣으면 **그 파일을 씁니다.** 없으면 앱이 소리를 만들어 냅니다. 자세한 규격은 `sounds/README.md` 에 있습니다.

**마스코트 · 성장**

```js
COMMON              // 씨앗·새싹·떡잎 (공통 단계)
PATHS               // { sol:{...}, kuk:{...} } 갈라진 뒤의 단계
WEARS               // 옷과 열리는 조건
SPLIT_AT            // 갈림길에 서는 기록 수 (지금 6)
getPath()/setPath() // 고른 친구
getWear()/toggleWear(id)
growthOf(기록수)      // → {지금, 다음, 번호, 목록}
mascotHTML(단계)      // 그림+옷을 겹친 HTML
```

`mascot/` 에 png 를 넣으면 이모지 대신 그림이 나옵니다. 규격은 `mascot/README.md` 에 있습니다.

**계정 (이 기기 안)**

```js
getProfiles()       // 이 기기에 등록된 사람들
rememberProfile()   // 지금 사람을 목록에 넣는다 (켤 때 저절로)
showLogin()         // «누구인가요?» 화면
loginAs(id)         // 그 사람으로 들어간다
setPin()            // 네 자리 잠금번호
toggleAskLogin()    // 공용 기기 모드 켜기/끄기
```

**잃어버린 기록 찾기**

```js
allRecordBoxes()    // 이 브라우저 안의 모든 기록 칸을 훑는다
renderRescue()      // 지금 칸이 비었는데 다른 칸에 기록이 있으면 알려 준다
rescueFrom(키)      // 그 칸에서 가져온다
```

**관리자**

```js
isAdmin()           // 관리자 모드가 열려 있나
openAdmin()         // 암호를 묻고 관리자 화면을 연다
renderAdmin()       // 관리자 화면을 다시 그린다
makeBackup()        // 이 기기의 bookcok_ 칸을 통째로 글자로 만든다
restoreBackup()     // 그 글자로 되살린다
```

**친구와 나누기**

```js
shareBook(책이름)     // 그 책의 카드를 만들고 '카드 보내기' 화면을 연다
buildCard(책이름)     // 내 기록에서 카드 하나를 만든다
cardHTML(카드, 내것인가) // 카드를 화면에 그릴 HTML
getFriends()         // 담아 둔 친구 카드 배열
renderFriends()      // 친구 책장을 다시 그린다
```

친구 카드는 **`K('friends')` 에 따로 담기고 내 기록과 절대 섞이지 않습니다.** 숫자 카드·성장 단계는 내 기록만 셉니다.

**독서 중**

```js
startReading(책이름)   // 독서 시작. 책이름은 생략 가능
pauseTimer()          // 일시정지 ↔ 이어서
finishReading(byUser) // 끝내고 기록 화면으로. 사람이 버튼을 눌렀으면 true
elapsedSec()          // 지금까지 읽은 초
isRunning()           // 시간이 흐르는 중인가
clearEntryForm()      // 기록 칸·읽은 시간·백색소음을 비운다 (저장하면 저절로 불린다)
```

기록을 저장하면 입력칸과 `timerSec` 이 **저절로 비워집니다.** 확장 블록에서 기록을 직접 넣을 때도, 사람이 쓴 칸을 읽어서 저장했다면 `clearEntryForm()` 을 한 번 불러 주세요. 안 그러면 다음에 쓴 기록에 **아까 읽은 시간이 그대로 붙습니다.**

**도구**

```js
esc(글자)    // 화면에 넣기 전에 반드시. < > & 를 안전하게 바꾼다
jsq(글자)    // onclick="..." 안에 이름을 넣을 때
fmt(초)      // 1830 → '30:30'
hhmm(분)     // 95 → '1h 35m'
isIOS()      // 아이폰·아이패드인가
```

**성장 단계** — 표 하나만 고치면 됩니다.

```js
const GROWTH=[
 {at:0,  face:'🌰', name:'씨앗',      말:'아직 심기 전이에요'},
 {at:1,  face:'🌱', name:'새싹',      말:'첫 기록이 싹을 틔웠어요'},
 ...
];
growthOf(기록수)  // → {지금, 다음, 번호}
```

`at` 은 그 단계가 되는 데 필요한 **기록 수**입니다. 단계를 더하거나 빼도 화면은 알아서 맞춰집니다.

### 주소 뒤에 붙는 것

**주소에 실린 이름·반은 조용히 적용되지 않습니다.** 친구가 보낸 링크로 들어간 사람이 그 친구가 되어 버리기 때문입니다. 처음 온 사람에게는 등록 화면에 **채워만** 주고, 이미 등록한 사람에게는 **물어봅니다** (기본은 내 계정 그대로). 읽고 나면 주소창에서 지웁니다.

```
?c=반이름     등록 화면에 반을 채워 준다 (이름 단계로 바로 간다)
?u=이름       등록 화면에 이름을 채워 준다 — 그대로 로그인되지는 않는다
?src=nfc     NFC 태그로 들어왔다 → 바로 독서 화면
?dnd=on      방해금지가 켜졌다고 표시
?book=제목    태그가 알려준 책 제목
?done=1      '독서 끝' 단축어가 방해금지를 끄고 돌아왔다
```

### 화면 모양 재활용

색과 모서리는 파일 맨 위 `:root` 안의 변수로 정해져 있습니다. **거기만 바꾸면 앱 전체의 인상이 같이 바뀝니다.**

```css
--brand:#2f6b4a;    /* 브랜드 초록 — 버튼·강조 */
--brand-l:#e9f2ea;  /* 옅은 초록 배경 */
--bg:#f2f4f0;       /* 바탕 */
--paper:#ffffff;    /* 카드 */
--ink:#1c2a22;      /* 본문 글자 */
--r-lg:22px;        /* 카드 모서리 */
```

새로 CSS를 만들기보다 이미 있는 것을 쓰면 앱이 따로 놀지 않습니다.

```
.card       흰 카드          .label   작은 회색 글씨
.primary    초록 버튼        .secondary  흰 버튼
.bar > i    진행 막대        .pill    작은 알약 표시
.empty      비었을 때 안내    .section  한 덩어리
```

---

## 6. ⛔ 건드리면 안 되는 것

**① `runEndShortcut()` 을 미뤄서 부르지 않는다**
`setTimeout` 으로 감싸면 사파리가 "사용자가 누른 것"으로 안 봐서 **막습니다.** 버튼을 누른 그 자리에서 바로 불러야 합니다.

**② `localStorage` 를 직접 쓰지 않는다 — `K()` 와 `put()` 을 거친다**
학교·반·사람마다 칸이 나뉘어 있습니다. `K('records')` 를 거치지 않으면 데이터를 못 찾거나 남의 칸을 덮어씁니다.
그리고 `localStorage.setItem` 은 **조용히 실패합니다** (사파리 사생활 보호 모드, 저장 공간 꽉 참). 그러면 «저장했습니다» 라고 말해 놓고 기록이 사라집니다. 반드시 `put(K('records'), ...)` 로 쓰세요.

**③ 화면을 다시 그릴 땐 `renderLibraryFallback()` 하나만 부른다**
예전에는 `renderLibrary()` 라는 함수가 하나 더 있었습니다. 한 번도 쓰이지 않는 IndexedDB용이었고, 예시 책 네 권(데미안·아몬드·모모·어린 왕자)이 박혀 있어서 **완독 버튼을 누르면 보관함이 그 예시 책들로 바뀌어 버렸습니다.** 지금은 지웠습니다.
살아 있는 것은 이름 끝에 **`Safe`** 나 **`Fallback`** 이 붙은 쪽뿐입니다. GPT가 `renderLibrary()` 나 `getRecords()` 를 부르는 코드를 주면 **파일에 없는 함수**이니 `renderLibraryFallback()` 과 `getRecordsSafe()` 로 바꿔 달라고 하세요.

**④ 위쪽 코드를 지우거나 바꾸지 않는다**
새 기능은 **맨 아래 확장 블록에만** 넣습니다. 기존 동작을 정말 바꿔야 하면, 같은 이름의 함수를 확장 블록에서 다시 정의해 덮어쓸 수 있습니다.

**⑤ 파일 전체를 다시 받지 않는다**
이 파일은 약 700줄, 50KB 입니다. GPT가 전체를 뱉으면 **중간이 잘려서 옵니다.** 조각만 받습니다.

---

## 7. GPT가 자주 내는 사고

| 증상 | 대처 |
|---|---|
| `…생략…` 이 보인다 | 그대로 쓰면 안 됨. "생략하지 말고 전체를 줘" |
| 파일을 통째로 다시 씀 | "맨 아래 확장 블록에 넣을 코드만 줘" |
| "저장됩니다" 라고 하는데 저장 안 됨 | 눌러서 확인. 말이 아니라 화면을 믿는다 |
| 앞 대화에 끌려간다 | 새 기능은 **새 대화**에서 시작 |
| 터미널 명령을 알려준다 | "나는 아이패드라 터미널이 없어" 다시 말하기 |
| 무료 한도가 찼다 | 쓰던 코드와 문장은 미리 복사해 두기 |

---

## 8. 올린 뒤 확인 순서

1. github.com 저장소에서 **파일이 바뀌었는지** 눈으로 확인
2. **1~2분 기다린다** (바로 안 바뀌는 게 정상)
3. 폰에서 새로고침 — 안 바뀌면 **탭을 닫았다 다시 연다** (캐시)

### 앱은 멀쩡한데 넣은 기능만 안 보인다

**이게 가장 흔한 증상입니다.** 확장 블록은 앱 본체와 분리돼 있어서, 여기에 문법 오류가 있어도 **앱은 아무 일 없다는 듯 잘 돕니다. 화면이 하얘지지 않습니다.** 오류 표시도 없습니다. 그래서 "올렸는데 아무 변화가 없다"로 보입니다.

아이패드에는 개발자 콘솔이 없으니 이렇게 확인합니다.

**① 코드가 실행은 되는지 본다** — 붙여넣은 코드 **맨 첫 줄**에 이걸 넣고 다시 올립니다.

```js
alert('여기까지 왔다');
```

- 알림창이 **뜨면** → 실행은 되는 것. 그 아래 코드의 내용이 문제
- 알림창이 **안 뜨면** → 문법 오류라 블록 전체가 안 돌고 있음. GPT에게 *"이 코드에 문법 오류가 있는지 봐줘"* 라고 그대로 보냅니다

**② 붙여넣은 위치를 확인한다** — 반드시 `▼▼▼` 와 `▲▲▲` **두 줄 사이**여야 합니다. 그 바깥에 붙으면 안 돕니다.

**③ 그래도 안 되면 되돌린다** — 붙여넣은 것을 전부 지우고 다시 Commit 하면 원래대로 돌아옵니다. **앱을 망가뜨릴 수 없는 구조**이니 마음 놓고 시도하세요.

---

## 9. 학교 · 학년 · 반 등록

### 선생님이 할 일은 두 가지

**① 학교 목록 넣기** — 화면 위 **⚙️ 더보기 ▸ 🔧 관리자** 에서 암호를 넣고, 학교 이름을 **한 줄에 하나씩** 붙여넣습니다. 넣어 두면 학생이 검색해서 고릅니다. 안 넣어도 학생이 직접 칠 수 있습니다.

**② 주소 하나 나눠주기**

```
https://내주소/
```

반마다 주소를 따로 만들 필요가 없습니다. 학생이 처음 열면 이렇게 진행합니다.

```
1. 학교   검색해서 고르기 (없으면 직접 입력)
2. 학년   1~3학년 (초등학교면 1~6학년)
3. 반     1~20반
4. 이름   김지우
        ↓
저장 칸: bookcok_records__세종고등학교 2-4__김지우
```

한 번만 하면 그다음부터는 홈 화면 아이콘으로 바로 들어갑니다.

### 알아 둘 것

| | |
|---|---|
| 반이 다르면 | 이름이 같아도 **완전히 다른 칸**입니다. 기록도 통계도 안 섞입니다 |
| 전에 쓰던 기록 | 사라지지 않습니다. 보관함에 **「예전 기록을 이 반으로 가져오기」** 가 뜹니다 |
| 바꾸고 싶으면 | 화면 위 **🏫 단추** 를 누르면 등록 화면이 다시 열립니다 |
| NFC 태그 | 태그 주소에도 학교·반이 자동으로 붙습니다 |
| 등록 화면 | **처음 온 사람에게만** 뜹니다. 이미 기록이 있는 사람은 방해하지 않습니다 |
| 반을 정해서 보내기 | `?c=세종고등학교 2-4` 주소로 보내면 **이름 단계로 바로** 갑니다 |
| 내 주소를 친구에게 | 안전합니다. 등록을 마치면 **주소창에 이름·반이 남지 않습니다** |

---

## 10. 관리자

화면 위 **⚙️ 더보기 ▸ 🔧 관리자**. 암호는 `index.html` 안 `ADMIN_CODE` 한 줄입니다.

### ⚠️ 이 암호는 비밀이 아닙니다

앱이 파일 하나로 되어 있어서 **마음먹고 들여다보면 누구나 찾을 수 있습니다.** 문을 잠그는 자물쇠가 아니라 **「관계자 외 출입금지」 팻말**입니다. 성적처럼 지켜야 할 것은 이 앱에 넣지 마세요.

```js
const ADMIN_CODE='bookcok-2026';           // ← 바꾸려면 여기
const ADMIN_SEED=['만든 사람 1','만든 사람 2']; // 관리자 이름 (앱 안에서도 바꿀 수 있음)
```

### 관리자 화면에서 되는 것

| | |
|---|---|
| 👥 관리자 | 두 사람 이름을 적어 둡니다 |
| 🏫 학교 목록 | 학생 등록 화면에 나올 학교를 정합니다 |
| 💾 이 기기 상태 | 저장이 되는지, 얼마나 쓰는지, 저장 칸 이름이 무엇인지 |
| 📦 백업 · 옮기기 | 기록을 통째로 글자로 뽑고, 그 글자로 되살립니다 |
| 📊 모인 카드 | 친구 책장 카드를 엑셀에 붙일 수 있는 표로 뽑습니다 |
| ⚠️ 초기화 | 지금 칸의 기록을 전부 지웁니다 |

### 🌐 「서버 관리」는 아직 없습니다

이 앱은 **서버가 없습니다.** 기록은 각자의 기기 안에만 있고, **관리자도 남의 기기 안은 볼 수 없습니다.**

| | |
|---|---|
| 지금 되는 것 | 학교 목록 정하기 · 이 기기 백업 · 카드로 모으기 |
| 서버가 있어야 되는 것 | 반 전체 실시간 순위 · 선생님이 모두의 기록 보기 · 기기를 바꿔도 따라오는 기록 |
| 서버를 붙이려면 | 무료 데이터베이스(파이어베이스 등) 계정과 로그인이 필요합니다 |

---

## 11. 마스코트와 성장

### 어떻게 자라나

```
씨앗 → 새싹 → 떡잎              ← 여기까지는 모두 같다
        ↓  기록 6개가 되면 갈림길
   🦔 솔이(고슴도치)  또는  🐷 꿀이(돼지)   ← 학생이 고른다
        ↓
   4단계까지 자란다 (기록 6 · 10 · 15 · 22개)
```

| | |
|---|---|
| 🦔 **솔이** | 고슴도치 · 돋보기를 든 탐정 |
| 🐷 **꿀이** | 돼지 · 머리에 꽃을 단 이야기꾼 |

한 번 고르면 그 친구로 계속 자랍니다. **성장 화면에서 「바꾸기」로 언제든 바꿀 수 있고, 기록과 단계는 그대로 남습니다.**

### 옷은 언제 입나

**안 입고 시작합니다.** 기록이 쌓이거나 책을 완독하면 옷장에 하나씩 열리고, 학생이 눌러서 입힙니다. 한 번에 한 개만 입습니다.

| 옷 | 열리는 조건 |
|---|---|
| 🧣 목도리 | 기록 6개 |
| 🎩 모자 | 완독 1권 |
| 👓 안경 | 기록 15개 |
| 🎀 리본 | 완독 3권 |
| 👑 왕관 | 기록 22개 |

### 그림

**이미 그림이 들어 있습니다.** 솔이·꿀이는 키링 그림에서 오려냈고, 씨앗·새싹·떡잎과 옷 5종은 같은 색·선 굵기로 그렸습니다.

바꾸려면 `mascot/` 에 **같은 이름으로 덮어쓰기만** 하면 됩니다. **코드는 안 고쳐도 됩니다.** 파일 목록과 규격은 `mascot/README.md` 에 있습니다.

옷은 캐릭터마다 얼굴·목 위치가 달라 **캐릭터별로 한 장씩** 둡니다 (`wear-scarf-sol.png` · `wear-scarf-kkul.png`). 공용 한 장만 쓰고 싶으면 `wear-scarf.png` 만 남기면 됩니다.

---

## 12. 백색소음

### 안 들린다고 할 때

앱 안 **독서 화면 ▸ 🔎 소리 확인** 을 누르면 앱이 직접 재서 알려 줍니다.

| 나오는 말 | 뜻 | 할 일 |
|---|---|---|
| ✅ 소리는 나가고 있습니다 | 앱은 정상 | **아이폰 옆면 무음 스위치**를 확인 — 무음이면 이 앱 소리는 아예 안 납니다 |
| ❌ 소리가 나가지 않고 있습니다 | 폰이 소리를 아직 안 열어 줌 | 소리 버튼을 한 번 더 누르기 |

**무음 스위치는 웹으로 넘을 수 없습니다.** 아이폰은 웹페이지 소리를 무음 스위치에 묶어 두고, 이것을 푸는 방법은 앱스토어 앱에만 있습니다.

### 소리 파일 넣기

`sounds/` 에 `rain.mp3` `cafe.mp3` `forest.mp3` `wave.mp3` 를 넣으면 **그 파일을 씁니다.** 없으면 앱이 만들어 냅니다. 규격은 `sounds/README.md` 에 있습니다.

---

## 13. 계정 — 서버 없이 할 수 있는 만큼

**진짜 로그인은 서버가 있어야 합니다.** 다른 기기에서 같은 아이디로 들어가려면 기록이 인터넷 어딘가에 있어야 하는데, 이 앱은 파일 하나라 그럴 곳이 없습니다.

그래서 **이 기기 안에서만 되는 계정**을 만들었습니다.

| | |
|---|---|
| 등록 | 학교·반·이름을 고르면 이 기기에 사람으로 기억됩니다 |
| 바꾸기 | ⚙️ 더보기 ▸ 👤 계정 바꾸기 |
| 잠금번호 | ⚙️ 더보기 ▸ 🔒 내 잠금번호 (네 자리, 안 걸어도 됩니다) |
| 공용 기기 모드 | 켜면 앱을 열 때마다 «누구인가요?» 를 묻습니다. 교실 태블릿용 |
| 기기 옮기기 | 관리자 화면의 **백업 글자**를 새 기기에 붙여넣습니다 |

**🔓 잠금번호는 비밀번호가 아닙니다.** 친구가 실수로 들어오는 것을 막는 정도이고, 마음먹고 들여다보면 풀 수 있습니다.

---

## 14. 친구와 나누기 — 서버 없이 되는 이유

반 친구끼리 기록을 나누려면 보통 **서버**가 필요합니다. 이 앱은 서버가 없습니다. 그래서 **카드를 보내는 대신, 카드 내용이 통째로 들어 있는 주소를 만들어 그 주소를 보냅니다.**

```
보관함 ▸ 책 누르기 ▸ 📤 이 책 카드 공유
  → 링크가 만들어진다 → 카톡·에어드랍·문자로 보낸다
      → 친구가 그 링크를 연다 → 카드가 뜬다 → 「친구 책장에 담기」
```

주소는 이렇게 생겼습니다. `#` 뒤는 **서버로 가지 않습니다.** 그래서 어디에도 기록이 남지 않습니다.

```
https://내주소/#c=eyJ2IjoxLCJ1Ijoi7KeA...
```

**알아 둘 것**

| | |
|---|---|
| 실시간이 아니다 | 링크를 보낸 그 순간의 카드입니다. 친구가 더 읽어도 내 화면은 안 바뀝니다 |
| 링크를 받은 사람은 누구나 본다 | 되돌릴 수 없습니다. 남에게 보이면 안 되는 생각은 빼고 보내세요 |
| 길이 제한 | 문장 100자, 생각 150자까지만 담습니다. 넘으면 앞부분만 갑니다 |
| 내 통계와 안 섞인다 | 받은 카드는 `K('friends')` 에 따로 담깁니다 |

**진짜 실시간 반 서재를 만들고 싶다면** 서버가 있어야 합니다. 이 파일 하나로는 안 됩니다. 그때는 선생님과 상의하세요.

---

## 15. 남한테 물려줄 때

이 앱을 다른 사람이 이어받으려면 저장소에 이 다섯 개가 있으면 됩니다.

```
index.html      앱 본체 (맨 아래에 확장 자리)
manifest.json   홈 화면에 앱으로 추가되게 하는 파일
icon-*.png      아이콘 4개
mascot/         마스코트 그림 (없어도 됩니다 — 이모지로 나옵니다)
sounds/         백색소음 mp3 (없어도 됩니다 — 앱이 만들어 냅니다)
인수인계.md      이 파일
CLAUDE.md       코드를 고칠 때 지킬 것
```

받는 사람은 **자기 GitHub 계정에 저장소를 만들고 이 파일들을 올린 뒤 Settings ▸ Pages ▸ main ▸ Save** 하면 자기 주소가 생깁니다. 앱 안의 **🔕 NFC 설정** 화면은 자기 주소를 알아서 계산해 보여줍니다 — 주소를 코드에 적어 둔 곳이 없습니다.
